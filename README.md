# 🌓 Theme Toggle Project

A interactive web application that demonstrates core **JavaScript** concepts, specifically focusing on the **Document Object Model (DOM)** and the **Browser Object Model (BOM)**.

## 🚀 Features
- **Dynamic Counter:** An interactive counter that increments and decrements values in real-time.
- **Theme Switcher:** A smooth toggle between Light and Dark modes using CSS classes and JavaScript logic.
- **Responsive Design:** Centered layout that works across different screen sizes.
- **BOM Integration:** Demonstrates script loading best practices and browser window interaction.

## 🛠️ Technologies Used
- **HTML5:** Semantic structure and layout.
- **CSS3:** Custom styling with transitions for smooth theme swapping.
- **JavaScript (ES6):** Logic for state management and DOM manipulation.

## 📂 Project Structure
- `index.html`: The main structure of the application.
- `index.js`: The logic for the counter and theme switching.
- `assets/`: (Optional) Folder for images or extra resources.

## 💡 Key Learnings
- **The `defer` Attribute:** Learned how to properly link JavaScript in the `<head>` without breaking DOM element selection.
- **ClassList API:** Used `add()`, `contains()`, and `replace()` to manage application states.
- **BOM vs DOM:** Understanding how to manipulate the webpage content (DOM) versus the browser environment (BOM).

## 🔧 How to Run Locally
1. Clone the repository:
   ```bash
   git clone [https://github.com/rahulpajjuri12/window-theme-change.git](https://github.com/rahulpajjuri12/window-theme-change.git)
